# Local Development Environment
# Safe to commit - contains only non-sensitive defaults

WORDPRESS_DB_HOST=db
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=wordpress
WORDPRESS_DB_NAME=wordpress

WP_URL=http://127.0.0.1:8080
WP_TITLE=Canadian Taxpayers Federation
WP_ADMIN_USER=admin
WP_ADMIN_PASS=admin123
WP_ADMIN_EMAIL=admin@example.com

# Production secrets (GCS, ACF) are in .env.local.secrets
# Run ./pull-secrets.sh to generate that file
